// Moved to components/shared/panel.tsx when SA-08 needed the same frame on
// the system, config, audit and growth pages. Re-exported so the analytics
// tabs keep their local import path.
export { Panel, Unmeasured } from "@/components/shared/panel"
