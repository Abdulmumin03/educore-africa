import { ComingSoon } from "@/components/dashboard/coming-soon"

export const metadata = { title: "User roles · EduCore Africa" }

export default function UserRolesPage() {
  return (
    <ComingSoon
      title="User roles"
      description="Granular permissions per role beyond the built-in 11 roles. For now, role assignment lives on the staff profile."
    />
  )
}
